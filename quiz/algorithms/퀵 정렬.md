---
category: algorithms
tag: Sort
---

#### 객관식 | [512]
퀵 정렬에 대한 설명으로 가장 알맞은 것은?
1. 항상 O(n)이다
2. ✅ 평균 시간복잡도는 O(n log n)이다
3. 최악이 O(log n)이다
4. 메모리 사용이 항상 O(n^2)이다
> 분할 정복 기반이며 피벗 품질이 성능에 큰 영향을 줍니다.

#### OX | [513]
퀵 정렬은 피벗 선택이 나쁘면 O(n^2)까지 느려질 수 있다.
> O
> 분할 정복 기반이며 피벗 품질이 성능에 큰 영향을 줍니다.

#### 빈칸 | [514]
퀵 정렬의 핵심 아이디어는 피벗 기준 ___(partition) 이다.
> partition
> 분할 정복 기반이며 피벗 품질이 성능에 큰 영향을 줍니다.
