---
category: data-structure
tag: Priority Queue
---

#### 객관식 | [539]
힙에 대한 설명으로 가장 알맞은 것은?
1. ✅ 최댓값(또는 최솟값)을 루트에서 O(1)에 확인한다
2. 임의 원소 탐색이 O(1)이다
3. 항상 완전 탐색이 필요하다
4. 정렬된 배열과 동일하다
> 힙은 우선순위 큐 구현에 많이 쓰이며 삽입/삭제는 O(log n)입니다.

#### OX | [540]
이진 힙에서 삽입 연산은 보통 O(log n)이다.
> O
> 힙은 우선순위 큐 구현에 많이 쓰이며 삽입/삭제는 O(log n)입니다.

#### 빈칸 | [541]
최대 힙에서 루트 노드는 항상 ___ 값이다.
> 최댓
> 힙은 우선순위 큐 구현에 많이 쓰이며 삽입/삭제는 O(log n)입니다.
