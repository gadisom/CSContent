---
category: network
tag: 역할
---

#### 빈칸 | [508]
DNS의 역할은 도메인 이름을 ___ 주소로 변환하는 것이다.
> IP
> DNS(Domain Name System)는 사람이 읽기 쉬운 도메인을 컴퓨터가 사용하는 IP로 변환합니다.

#### 객관식 | [572]
DNS에 대한 설명으로 가장 알맞은 것은?
1. 패킷 암호화를 수행한다
2. 라우팅 경로를 계산한다
3. 포트 번호를 할당한다
4. ✅ 도메인 이름을 IP 주소로 변환한다
> DNS는 인터넷의 전화번호부 역할을 합니다.

#### OX | [573]
DNS 조회 결과는 캐시를 통해 재사용될 수 있다.
> O
> DNS는 인터넷의 전화번호부 역할을 합니다.

#### 빈칸 | [574]
DNS 레코드 중 IPv4 주소 매핑은 ___ 레코드다.
> A
> DNS는 인터넷의 전화번호부 역할을 합니다.
