---
category: network
tag: 상태 코드
---

#### 객관식 | [501]
HTTP 상태 코드 404가 의미하는 것은?
1. 서버 내부 오류
2. ✅ 요청 리소스 없음
3. 권한 없음
4. 요청 성공
> 404 Not Found: 요청한 리소스가 서버에 존재하지 않습니다.

#### 객관식 | [569]
HTTP의 기본 동작 방식으로 가장 알맞은 것은?
1. 항상 상태를 서버가 유지한다
2. TCP를 사용하지 않는다
3. ✅ 기본적으로 무상태(stateless) 프로토콜이다
4. 응답 코드는 없다
> HTTP는 요청-응답 기반이며 상태 관리는 별도 메커니즘이 필요합니다.

#### OX | [570]
세션/쿠키는 HTTP의 무상태 특성을 보완하기 위해 사용된다.
> O
> HTTP는 기본적으로 요청 사이의 상태를 기억하지 않으므로 쿠키와 세션 같은 별도 상태 관리가 필요합니다.

#### 빈칸 | [571]
HTTP 상태코드 200은 ___ 를 의미한다.
> 성공
> 200 OK는 요청이 성공적으로 처리되었음을 의미하는 대표적인 2xx 상태 코드입니다.
